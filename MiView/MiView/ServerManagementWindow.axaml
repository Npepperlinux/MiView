<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="MiView.ServerManagementWindow"
        Title="サーバー管理"
        Width="500"
        Height="400"
        WindowStartupLocation="CenterOwner"
        Background="#F0F0F0">
    <Window.Styles>
        <Style Selector="TextBox">
            <Setter Property="SelectionBrush" Value="#86b300" />
            <Setter Property="SelectionForegroundBrush" Value="White" />
        </Style>
    </Window.Styles>
    
    <Grid Margin="10" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- ヘッダー -->
        <TextBlock Grid.Row="0" Text="登録済みサーバー一覧" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="#000000" Background="Transparent"/>
        
        <!-- サーバーリスト -->
        <Border Grid.Row="1" BorderBrush="#8C8C8C" BorderThickness="1" Background="#FFFFFF">
            <ListBox x:Name="serverListBox" Background="#FFFFFF" Foreground="#000000" BorderBrush="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Background="#FFFFFF">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="5" Background="Transparent">
                                <TextBlock Text="{Binding InstanceName}" FontWeight="Bold" FontSize="12" Foreground="#000000" Background="Transparent"/>
                                <TextBlock Text="{Binding ApiKeyStatus}" FontSize="10" Foreground="#666666" Background="Transparent"/>
                            </StackPanel>
                            
                            <Button Grid.Column="1" Content="編集" Margin="5" Padding="10,3" 
                                    Background="#FFFFFF" Foreground="#000000" BorderBrush="#8C8C8C" BorderThickness="1"
                                    Click="EditServer"/>
                            <Button Grid.Column="2" Content="削除" Margin="5" Padding="10,3"
                                    Background="#FFEBEE" Foreground="#D32F2F" BorderBrush="#F44336" BorderThickness="1" 
                                    Click="DeleteServer"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        
        <!-- ボタン -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="サーバー追加" Margin="0,0,10,0" Padding="15,5"
                    Background="#FFFFFF" Foreground="#000000" BorderBrush="#8C8C8C" BorderThickness="1"
                    Click="AddServer"/>
            <Button Content="閉じる" Padding="15,5"
                    Background="#FFFFFF" Foreground="#000000" BorderBrush="#8C8C8C" BorderThickness="1"
                    Click="CloseWindow"/>
        </StackPanel>
    </Grid>
</Window>